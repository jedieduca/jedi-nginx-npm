services:
  nginx-proxy:
    image: 'jc21/nginx-proxy-manager:latest'
    container_name: jedi_nginx_proxy
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
      - '81:81'
    volumes:
      - ./nginx/data:/data
      - ./nginx/letsencrypt:/etc/letsencrypt
      - ${CAMINHO_PROJETO_ADIANTI}:/var/www/html
      # Uma única montagem já engloba todo o código e a pasta storage/app/public:
      - ../jedi-site/backend/site:/var/www/html/site
      - graficos_data:/var/www/html/mad/app/output/charts:ro
    networks:
      jedieduca-network:
        aliases:
          - api.dev.jedieduca.com.br

volumes:
  graficos_data:
    name: jedi-python_graficos_data
    external: true

networks:
  jedieduca-network:
    external: true